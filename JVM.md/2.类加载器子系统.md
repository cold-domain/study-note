# 2.类加载器子系统

![image](assets/image-20260325161628-pp8jyjc.png)

‍

‍

‍

一、内存结构概述

**栈区中的每一页是叫栈帧（Stack Frame）** 

![image](assets/image-20260325160754-oror43s.png)

![image](assets/image-20260325161137-9gt44ga.png)

![image](assets/image-20260325161219-pdjiewg.png)

‍

‍

Q：手写java虚拟机要考虑的结构？

A：**类加载器和执行引擎**

![image](assets/image-20260325161425-074q8lc.png)

![image](assets/image-20260325161413-hc8duqf.png)

‍

‍

‍

二、类加载器与类的加载过程

1.类加载器子系统的作用

类加载器子系统负责**从文件系统或者网络中加载class文件**，class文件在文件开头有**特定的文件标识**。 **（咖啡宝宝：CA FE BA BE）** 

**ClassLoader只负责class文件的加载**，至于它**是否可以运行，则由Execution Engine（执行引擎）决定**。

加载的**类信息存放于一块称为方法区的内存空间**。除了类的信息外，**方法区中还会存放运行时常量池信息**，可能还包括字符串字面量和数字常量 **(这部分常量信息是Class文件中常量池部分的内存映射)** 

**运行时常量池：** **常量池加载到内存中**

**jdk8.0，永久代被元空间取代（替代了原本的PermGen转而使用了元空间实现方法区）。常量池也分全局字符串池，class文件常量池，和方法区常量池。** 

![image](assets/image-20260325164914-fy7c17p.png)

![image](assets/image-20260325165345-xhzkqdi.png)

‍

例子：

1.xxx.class文件由类加载器加载到内存中，形成DNA元数据模板，放在方法区，之后就可以实例化

2.类加载过程

![image](assets/image-20260325165612-2phgzyz.png)

![image](assets/image-20260325170007-9qfxhul.png)

‍

‍

2.类的加载过程

类的加载过程一——Loading（加载阶段）

**加载：**

**1.通过一个类的全限定名获取定义此类的二进制字节流**

**2.将这个字节流所代表的静态存储结构转化为方法区的运行时数据结构**

**3.在内存中生成一个代表这个类的java.lang.Class对象，**​**作为方法区这个类的各种数据的访问入口**

PS：

1.**方法区改名为元空间（JDK8后）** 

2.**存放到方法区的数据是元数据，而class对象（最终产品）存放到堆区，二者不相等**；方法区主要存放的是类的方法代码，变量名等信息（元空间）

![image](assets/image-20260325170452-1xp6pvm.png)

‍

‍

加载.class文件的方式：

![image](assets/image-20260325171002-en1wmql.png)

‍

‍

类的加载过程二——链接

验证：

目的在于**确保Class文件的字节流中包含信息符合当前虚拟机要求**，保证被加载类的正确性，不会危害虚拟机自身安全

**主要包括四种验证，文件格式验证，元数据验证，字节码验证，符号引用验证。** 

加载：**为类变量分配内存并且设置该类变量的默认初始值，即零值**。（赋具体值在初始化阶段）

**这里不包含用final修饰的static，因为final在编译的时候就会分配了，准备阶段会显式初始化；** 

**这里不会为实例变量分配初始化**，**类变量会分配在方法区中**，而**实例变量是会随着对象一起分配到Java堆中**。

解析：**将常量池内的符号引用转换为直接引用的过程。** 

**事实上，解析操作往往会伴随着JVM在执行完初始化之后再执行**

![image](assets/image-20260325171956-dhf628c.png)

‍

‍

类的加载过程三——初始化：

初始化:  
**1.初始化阶段就是执行类构造器方法<clinit>()的过程。** 

2.此方法**不需定义**，是javac编译器**自动收集类中的所有****类变量（静态变量）** **的赋值动作和静态代码块中的语句合并而来**。

3.构造器方法中指令按语句在源文件中出现的顺序执行。

**4.<clinit>()不同于类的构造器**。(关联:构造器是虚拟机视角下的<init>())

5.**若该类具有父类，JVM会保证子类的<clinit>()执行前，父类的<clinit>()已经执行完毕**。

6.**虚拟机必须保证一个类的<clinit>()方法在多线程下被同步加锁**。 **（保证只加载一次而不加载多次）** 

![image](assets/image-20260325173950-w6n7n9t.png)![image](assets/image-20260325174142-jnojzea.png)

![image](assets/image-20260325174631-n3xg6tr.png)

![image](assets/image-20260325174806-d0cfr9e.png)

‍

‍

3.类加载器的分类

**注意jdk9之后已经没有extClassLoader了，改成PlatformClassLoader了**

**JVM支持两种类型的类加载器**，分别为**引导类加载器(BootstrapClassLoader) 和自定义类加载器 (User-Defined ClassLoader)** 

从概念上来讲，自定义类加载器一般指的是程序中由开发人员自定义的一类类加载器，但是Java虚拟机规范却没有这么定义，而是**将所有派生于抽象类ClassLoader的类加载器都划分为自定义类加载器。** 

**类加载器的关系是加载顺序关系，是包含关系** **（类似于文件夹）**  **，非上下层 / 继承关系**

**引导类加载器是c / c++编写的，而自定义类加载器是java编写的**

![image](assets/image-20260325175946-53rmwrw.png)

![image](assets/image-20260325175530-suw3rcw.png)

‍

例子：

对于**用户自定义类**来说，**默认使用系统类加载器加载**

对于**java的核心类库**来说，**都是使用引导类加载器进行加载**

![image](assets/image-20260325180351-pecv01c.png)

![image](assets/image-20260325180703-jns3duq.png)

![image](assets/image-20260325180932-tvpft1s.png)

‍

‍

4.虚拟机自带的加载器

启动类加载器（引导类加载器）（C++编写）：**正常获取不到**

**扩展类加载器和系统类加载器都****间接继承****了ClassLoader，而引导类加载器使用c语言实现的，比ClassLoader更深一层**

![image](assets/image-20260325182900-0ikuxfa.png)

‍

‍

扩展类加载器：

**这里的父类加载器并不是指的JAVA中的继承关系，** **而是指这个类是由哪个加载器加载进内存的**

![image](assets/image-20260325183035-rtu4n1d.png)

‍

‍

应用程序类加载器（系统类加载器）：

**该类加载是程序中默认的类加载器**

![image](assets/image-20260325183224-6wl3qf5.png)

‍

‍

5.用户自定义类加载器

自定义类加载器：

场景：

1.隔离加载类（中间件防止冲突）；2.修改类加载的方式（动态加载）；3.扩展加载源；4.防止源码泄漏

![image](assets/image-20260325183942-gh4z253.png)

‍

‍

实现步骤：

1.继承classloader类

2.重写loadclass方法（JDK1.2之前） / 编写findclass方法

3.可以直接继承URLClassLoader类，简洁

![image](assets/image-20260325184119-0didm8t.png)

![image](assets/image-20260325184355-3hnqsdc.png)

![image](assets/image-20260325184453-hll4r5x.png)

‍

‍

6.classloader

ClassLoader类：**它是一个抽象类** **，**​**其后所有的类加载器都继承自ClassLoader** **(不包括启动类加载器)** 

![image](assets/image-20260325184822-31lwbiq.png)

‍

‍

获取演示：

![image](assets/image-20260325185038-pndovyi.png)

![image](assets/image-20260325185030-mqbf5m2.png)

‍

‍

**7.双亲委派机制**

Java虚拟机对class文件采用的是**按需加载**的方式，也就是说当需要**使用该类时才会将它的class文件加载到内存生成class对象**。而且**加载某个类的class文件**时，Java虚拟机采用的是**双亲委派模式**，即**把请求交由父类处理，它是一种任务委派模式**。

**工作原理：** **儿子们都很懒  干什么都先找爸  爸干不了的才自己想办法**

1.如果一个**类加载器收到了类加载请求**，它并**不会自己先去加载**，而是**把这个请求委托给父类的加载器去执行**；

2.如果父类加载器**还存在其父类加载器，则进一步向上委托，依次递归**，**请求最终将到达顶层的****启动类加载器**；

3.如果**父类加载器可以完成类加载任务，就成功返回，倘若父类加载器无法完成此加载任务，子加载器才会尝试自己去加载**，这就是双亲委派模式。

**优势：** 

**1.避免类的重复加载**

**2.保护程序安全，防止核心API被随意篡改**

![image](assets/image-20260325202402-xhozxo7.png)

‍

例子1：

![image](assets/image-20260325202927-j4xwdne.png)

‍

例子2：

**反向委托**

**第三方类加载器由系统类加载器加载**

![image](assets/image-20260325203211-wh6qccr.png)

‍
