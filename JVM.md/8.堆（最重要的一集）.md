# 8.堆（最重要的一集）

![image](assets/image-20260325221912-lqd0ars.png)

![image](assets/image-20260325224347-a2e5faa.png)

‍

‍

‍

一、堆的核心概述

1.堆的核心概述

一个Java程序对应一个进程，一个进程对应一个JVM实例，一个JVM实例对应一个运行时数据区，**一个运行时数据区只有一个方法区和堆，** 一个进程的**多个线程共享同一个方法区和堆，** **每个线程有独立的程序计数器、虚拟机栈、本地方法栈**

**Xxs（Xxx）调整栈大小，Xms（Xmx）调整堆大小**

**jdk中有工具：jvisualvm，查看进程**

**一个JVM实例只存在一个堆内存，堆也是Java内存管理的核心区域。** 

**Java堆区在JVM启动的时候即被创建，其空间大小也就确定了**。**是JVM管理的最大一块内存空间**。

**堆内存的大小是可以调节的：**  **-Xms（Xmx）调整堆大小**

![image](assets/image-20260325230502-4t6nlyl.png)

![image](assets/image-20260325225523-idx73t5.png)

![image](assets/image-20260325230224-xy56hco.png)

![image](assets/image-20260325230432-0ze4d5e.png)

‍

‍

《Java虚拟机规范》规定，堆可以处于**物理上不连续**的内存空间中，但在**逻辑上**它应该被视为**连续的**。

**所有的线程共享Java堆**，在这里还可以划分**线程私有的缓冲区(Thread Local Allocation Buffer，TLAB）** 

也就是说，**堆的完整空间并不是共享的，而是大部分共享，小部分是分给线程独立的**

**栈上分配，逃逸分析，标量替换****会导致对象实例并不是在堆上分配空间，而是栈上**

**GC是需要消耗系统资源的，就类似于数据库连接的创建销毁，所以不建议频繁GC**

**gc会触发STW（stop the world），老年代的GC才会触发STW  年轻代GC不会**

**数组和对象可能永远不会存储在栈上，因为栈帧中保存引用，这个引用指向对象或者数组在堆中的位置。** 

**在方法结束后，堆中的对象不会马上被移除，仅仅在垃圾收集的时候才会被移除。** 

**堆，是GC(Garbage Collection，垃圾收集器)执行垃圾回收的重点区域。** 

![image](assets/image-20260325231139-utm7lc0.png)

![image](assets/image-20260325233111-0s4hsrk.png)

![image](assets/image-20260325231734-4p676qi.png)

![image](assets/image-20260325233057-yqx3c7o.png)

‍

‍

2.堆的核心概述：内存细分

**新生区、养老区、元空间（永久代）** 

**永久代的空间不归堆空间管理** **（归方法区管理）**  **，计算也不用算永久代的大小**

![image](assets/image-20260325233517-jtorgu8.png)

![image](assets/image-20260325233614-xqym8d6.png)

![image](assets/image-20260325233937-gem2yqg.png)

![image](assets/image-20260325234112-ufrr7lt.png)

‍

‍

3.设置堆内存大小与OOM

通过-Xms和-Xmx来设置堆区的起始内存和最大内存

一旦堆区中的**内存大小超过“-Xmx"所指定的最大内存**时，将会抛出**OutOfMemoryError（OOM）异常**。

通常**会将-Xms和-Xmx两个参数配置相同的值**，其**目的是为了能够在java垃圾回收机制清理完堆区后不需要重新分隔计算堆区的大小，从而提高性能**。

默认情况：

**初始内存大小：物理电脑内存大小 / 64**

**最大内存大小：物理电脑内存大小 / 4**

**开发中建议将初始堆内存和最大的最内存设置成相同值**

**新生代：幸存者区（S0S1区）+伊甸园区（E区）**

**老年代：老年区（O区）**

![image](assets/image-20260326154740-dxtaito.png)

![image](assets/image-20260326155203-r3pwyr0.png)

![image](assets/image-20260326155548-5oyyivg.png)

‍

‍

例子：

![image](assets/image-20260326160150-zmras0d.png)

![image](assets/image-20260326160450-x1en5gz.png)

‍

‍

查看设置的参数：

方式一：`jps / jstat -gc 进程id`

方式二：加上虚拟机参数`-XX:+PrintGCDetails`

![image](assets/image-20260326161136-0knpea2.png)

‍
