# 9.方法区

![image](assets/image-20260326161659-nll7sp5.png)

![image](assets/image-20260326161710-x891ga6.png)

‍

‍

‍

四、方法区的内部结构

1.方法区的存储——上：

它用于存储已被虚拟机加载的**类型信息、常量、静态变量、即时编译器编译后的代码缓存等。** 

方法区**除了记录class文件，还记录了class文件是被谁加载的（记录了对应的类加载器）** 

![image](assets/image-20260326163852-yyhwori.png)

![image](assets/image-20260326164054-yaycgjz.png)

‍

‍

类型信息：

对每个**加载的类型(类class、接口interface、枚举enum、注解annotation)** ，JVM必须在方法区中存储以下类型信息：  
1.**这个类型的完整有效名称** **(全名=包名.类名)** 

2.这个类型**直接父类的完整有效名**(对于**interface或是java.lang.Object，都没有父类**)

3.这个类型的**修饰符**(**public,abstract，final的某个子集**)

4.这个类型**直接接口的一个有序列表**

![image](assets/image-20260326164458-w1w499l.png)

‍

‍

域信息：

JVM必须在方法区中**保存类型的所有域的相关信息以及域的声明顺序**。

**域的相关信息包括：** **域名称、域类型、域修饰符**(**public,private,protected,static,final,volatile, transient的某个子集**)

![image](assets/image-20260326164450-iv8e8kc.png)

‍

‍

方法信息：

JVM必须保存**所有方法的以下信息，同域信息一样包括声明顺序方法名称**：

1.方法的**返回类型**(或void）

2.方法**参数的数量和类型**(按顺序)

3.方法的**修饰符**(public, private, protected, static, final,synchronized, native, abstract的一个子集)

4.方法的**字节码**(bytecodes)、**操作数栈、局部变量表及大小**(**abstract和native方法除外**)

5.**异常表** (abstract和native方法除外)

**每个异常处理的开始位置、结束位置、代码处理在程序计数器中的偏移地址、被捕获的异常类的常量池索引**

**个人理解方法区的是局部变量表的元数据，栈帧中的局部变量表保存的是变量的值**

**这里面相当于保存了类的模子**

**从class字节码文件层面来看，无论是构造器方法还是普通方法，都是<init>方法，而在代码则是分开的**

**在java源文件中，构造器不是方法；而在字节码文件中，构造器是<init>方法。** 

**类型信息、域信息、方法信息通过类加载器子系统加载到jvm运行时数据区的方法区中。** 

![image](assets/image-20260326164630-y57vkir.png)

‍

‍

例子：

**这里应该是方法的信息，再调用这个方法的时候栈帧里才是具体的数据**

![image](assets/image-20260326165717-uj47sit.png)

![image](assets/image-20260326165747-bvvok0n.png)

![image](assets/image-20260326170149-w70jjtr.png)

‍

‍

2.方法区的存储——下：

**non-final的类变量：** 

**静态变量和类关联在一起，随着类的加载而加载，它们成为类数据在逻辑上的一部分**。

**类变量被类的所有实例共享，即使没有类实例时你也可以访问它**。

![image](assets/image-20260326171345-5u45wo5.png)

‍

‍

**全局常量：static final**

**被声明为final的类变量的处理方法则不同，每个全局常量在编译的时候（字节码文件阶段）就会被分配了。** 

![image](assets/image-20260326171333-ufjle9i.png)

‍

‍

3.运行时常量池与常量池

**方法区**，内部包含了**运行时常量池**。

**字节码文件**，内部包含了**常量池**。

要弄清楚**方法区**，需要**理解清楚ClassFile**，因为**加载类的信息都在方法区**。

要弄清楚**方法区的运行时常量池**，需要理解清楚**ClassFile中的常量池**。

**常量池  ——>（加载）——>  运行时常量池**

![image](assets/image-20260326171959-bayna7x.png)

‍

‍

常量池：

一个有效的**字节码文件**中除了**包含类的版本信息、字段、方法以及接口等描述信息**外，还包含一项信息那就是**常量池表**(Constant Pool Table)，**包括各种字面量和对类型、域和方法的符号引用**。

![image](assets/image-20260326172603-ks9kfam.png)


‍

为什么需要常量池？

**说白了就是通过动态链接指向常量池中的引用**

**即class常量池加载到运行时常量池，之后运行时常量池指向了方法区中存储的类的元数据、堆中对象等地址**

![image](assets/image-20260326172912-ex4oab0.png)

‍

例子：常量池的使用

不断调用，不断指向

**常量池相当于原料，而代码相当于菜品，代码不断调用常量池，最后做成菜**

![image](assets/image-20260326174144-c8rqtib.png)

![image](assets/image-20260326174512-32qn1rj.png)

‍

‍

常量池存储的数据类型：

**数量值；字符串值；类引用；字段引用；方法引用**

![image](assets/image-20260326174631-kbh19tu.png)

![image](assets/image-20260326174856-fdrxu7m.png)

‍

‍

小结：常量池

![image](assets/image-20260326174917-wsdxx2g.png)

‍

‍

4.运行时常量池（常量池加载到方法区的动态空间）

**运行时常量池**(Runtime Constant Pool)是**方法区的一部分**。

**常量池表**(Constant PoolTable)**是Class文件的一部分**，**用于存放编译期生成的各种字面量与符号引用**，**这部分内容将在类加载后存放到方法区的运行时常量池中**。

**运行时常量池，在加载类和接口到虚拟机后，就会创建对应的运行时常量池**。

**JVM为每个已加载的类型(类或接口)都维护一个常量池**。池中的数据项像数组项一样，是通过**索引访问**的。

运行时常量池中包含多种不同的常量，包括编译期就已经明确的数值字面量，也包括到运行期解析后才能够获得的方法或者字段引用。此时不再是常量池中的符号地址了，这里换为真实地址。

**运行时常量池**，相对于Class文件常量池的另一重要特征是：**具备动态性**。**可以动态加载需要的类等等**

**运行时常量池**类似于传统编程语言中的**符号表**(symmboltable)，但是它所**包含的数据却比符号表要更加丰富一些**。

当创建类或接口的运行时常量池时，如果构造运行时常量池所需的内存空间超过了方法区所能提供的最大值，则JVM会抛outOfMemoryError（OOM）异常。

![image](assets/image-20260326175401-mbgb7jx.png)

![image](assets/image-20260326175129-6gopfb8.png)

‍

‍

5.方法区使用举例

存储和读取：

![image](assets/image-20260326184354-t24nd2i.png)

![image](assets/image-20260326184411-r3csju1.png)

![image](assets/image-20260326184448-sg7i6fc.png)

![image](assets/image-20260326184444-oc23u2u.png)

![image](assets/image-20260326184520-y6dxcgn.png)

![image](assets/image-20260326184604-zbcj3p2.png)

‍

‍

常量池：

**符号引用：就是类的全名，靠着这个就可以去找类**

**转换为真实的直接引用，只用直接引用和执行引擎配合才能执行代码**

**常量池通过符号引用寻找类的位置，之后转换为真实的直接引用，再执行代码**

**就跟springboot去容器里找bean是一个意思**

**getstatic应该是拿到对应的实例，invokevirtual应该是调用具体的方法**

![image](assets/image-20260326185031-8vfjilb.png)

![image](assets/image-20260326185336-n2cmhkv.png)

![image](assets/image-20260326185532-leftrjx.png)

‍
